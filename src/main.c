#include "my_box.h"

int main(int argc, char **argv)
{
  // int fd;

  // if (argc < 2)
  //   return (usage(argv[0]));
  // if ((fd = my_open(argv[1])) < 0)
  //   return (fd);
  return (my_box(0));
}
